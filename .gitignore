node_modules
dist
.env
.env.local
*.db
*.db-journal
# Copies taken before a schema change; `*.db` alone does not match them.
*.db.backup*
.DS_Store
coverage
build
.agents
apps/server/data/hls_cache/
apps/server/data/subtitle_cache/
apps/server/data/preview_cache/

# Generated by `pnpm prisma:generate`; CI and production builds regenerate it.
apps/server/src/generated/prisma/

# Playwright / end-to-end artefacts
/test-results/
/playwright-report/
/blob-report/
# Runtime data only: the application, test and end-to-end databases plus the
# generated e2e media. Listing the directory rather than each filename also
# covers the backup copies taken before a schema change.
apps/server/prisma/data/
.claude
.codex-home-hero
.codex-ios-ui-fix
.codex-ios-youtube-fix
.codex-cine-drive-ios-staging
